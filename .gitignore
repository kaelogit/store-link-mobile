# 🏰 STORELINK SYSTEM PROTECTION v76.1
# Hardened for: Secret Containment, Environment Privacy, and Multi-Platform Safety.

# 📦 DEPENDENCIES
node_modules/
.pnp
.pnp.js

# 🚀 EXPO & METRO
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# ☁️ SUPABASE CONTROL (Manifest Section VI)
# Prevents local database state and function secrets from leaking
.supabase/
supabase/.temp/
supabase/functions/.env
supabase/.branch

# 🔑 SECRETS & KEYS (CRITICAL SECURITY)
# Blocks all environment files and native signing certificates
.env
.env.*
!.env.example
*.pem
*.key
*.p8
*.p12
*.jks
*.mobileprovision

# 🤖 NATIVE BLUEPRINTS
# Native folders are ignored to ensure clean Expo Prebuilds
/ios
/android

# 🛠️ DEBUG & LOGS
npm-debug.*
yarn-debug.*
yarn-error.*
.pnpm-debug.log*

# 🏗️ TYPOGRAPHY & TYPESCRIPT
*.tsbuildinfo

# 🍏 OS SPECIFIC (System Debris)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db